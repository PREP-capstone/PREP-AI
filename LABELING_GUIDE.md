# 라벨링 가이드

`pilot_all_labeled_completed.csv` 및 신규 수집 데이터(`google_play_apps_raw.csv` 등)에
라벨을 매길 때 사용하는 2축 분류 기준입니다.

## 축1 — category_id (질병/도메인축, 8종 + 예외)

| id | 이름 | 설명 / 예시 |
|----|------|------|
| 0 | 수면 | 수면 사운드, 백색소음, 수면 기록·분석 |
| 1 | 정신건강 | 명상, 감정일기, 스트레스·불안 관리 |
| 2 | 운동 | 만보기, 운동 기록, 홈트레이닝 |
| 3 | 식단 | 칼로리 계산, 단백질/영양 트래킹 |
| 4 | 만성질환 | 혈압·혈당 기록, 종합 건강관리 |
| 5 | 여성건강 | 생리주기, 배란일, 임신·출산 |
| 6 | 유전자 | 유전자 검사/분석 리포트 |
| 7 | 미용 | 피부 측정, 시술 정보/매칭 |
| EXC | 예외 | 특정 병원 전용 앱, 병원비교/예약 등 일반의료비교, 건강과 무관한 콘텐츠 |

## 축2 — function_type (기능축, 4종)

| code | 이름 | 설명 / 판단 포인트 |
|------|------|------|
| A | 정보제공 | 사용자는 콘텐츠를 소비만 함 (재생, 큐레이션, 정보 열람) |
| B | 데이터기록관리 | 사용자가 직접 데이터를 기록·추적·관리 (가장 흔한 유형) |
| C | 매칭연결 | 전문가/시술/병원 매칭, 가격비교, 예약·연결 서비스 |
| D | 개입치료 | 실시간 피드백으로 행동을 능동적으로 유도 (적응형 코칭, 알림 기반 개입) |

한 앱이 여러 기능을 겸할 경우, **가장 핵심적으로 강조되는 기능**을 기준으로 판단합니다.
애매한 경계 사례는 `note` 컬럼에 판단 근거를 한 줄로 남기고, `is_boundary_case`를 `True`로 표시합니다.

## 라벨링 절차 (신규 데이터용)

1. `predict_labels.py`로 학습된 모델의 예측값(`predicted_category_id`, `predicted_function_type`)과
   확신도(`category_confidence`, `function_confidence`)를 먼저 생성합니다.
2. 확신도가 낮은 순으로 정렬되어 있으므로 위에서부터 검토하며 `category_id`/`function_type`을
   확정합니다. 확신도가 높은 예측은 스팟체크만 해도 무방합니다.
3. `minority_candidate` 컬럼에 표시된 유전자(6)/미용(7) 후보를 우선 검토합니다 — 두 클래스는
   기존 데이터셋에서 표본이 극히 적어(각 17건, 25건) 보강 효과가 가장 큽니다.
4. 최종 확정한 라벨만 `category_id`/`function_type` 컬럼에 채우고, `predicted_*`/`*_confidence`/
   `minority_candidate` 컬럼은 병합 전에 제거합니다.
